const initialState = {
    param: ''
}

// const PARAM = 'PARAM';

export function giveParam(  ) {

}

function reducer( state = initialState, action ) {
    switch( action.type ) {

    }

    return state;
}

export default reducer;